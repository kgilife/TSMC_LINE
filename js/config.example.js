/**
 * MGM System - Configuration Template
 * Copy this file to `js/config.js` and fill in your actual URLs.
 */
const CONFIG = {
  // Your Google Apps Script Web App URL (Deploy -> Web app)
  GAS_WEB_APP_URL: "YOUR_GAS_WEB_APP_URL_HERE",
  
  // Default Target URL (e.g. LINE Official Account / Promotion Landing Page)
  TARGET_URL: "YOUR_TARGET_URL_HERE"
};
